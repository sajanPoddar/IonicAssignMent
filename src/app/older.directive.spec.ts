import { OlderDirective } from './older.directive';

describe('OlderDirective', () => {
  it('should create an instance', () => {
    const directive = new OlderDirective();
    expect(directive).toBeTruthy();
  });
});
